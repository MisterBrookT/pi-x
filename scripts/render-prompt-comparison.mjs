import { readFile, writeFile } from "node:fs/promises";
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";
const root=join(dirname(fileURLToPath(import.meta.url)),"..");
const pi=await readFile(join(root,"docs/prompts/pi-default.txt"),"utf8");
const omp=await readFile(join(root,"docs/prompts/omp-default-template.txt"),"utf8");
const esc=s=>s.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;");
const words=s=>s.trim().split(/\s+/).filter(Boolean).length;
const html=`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Full system prompts — Pi vs OMP</title><style>:root{color-scheme:dark;--bg:#0b0c0d;--s:#151719;--s2:#1e2124;--t:#f1f2f3;--m:#9da3a9;--line:#2d3237;--pi:#9fe5b4;--omp:#f0ca78}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--t);font:14px/1.55 Inter,-apple-system,sans-serif}.wrap{width:min(1500px,calc(100% - 28px));margin:auto}header{padding:52px 0 28px}h1{font-size:clamp(36px,6vw,72px);line-height:1;letter-spacing:-.055em;font-weight:500;margin:12px 0 18px}.lead{color:var(--m);font-size:18px;max-width:900px}.note{background:var(--s);padding:18px 20px;border-radius:10px;margin-top:22px}.stats{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:24px 0}.stat{background:var(--s);border-radius:10px;padding:16px}.stat b{font-size:28px;font-weight:500;display:block}.pi{color:var(--pi)}.omp{color:var(--omp)}.toolbar{position:sticky;top:0;background:rgba(11,12,13,.95);backdrop-filter:blur(14px);padding:10px 0;z-index:2}.toolbar input{width:100%;height:40px;border:0;border-radius:8px;background:var(--s2);color:var(--t);padding:0 13px;font:14px monospace}.cols{display:grid;grid-template-columns:1fr 1fr;gap:10px;padding:12px 0 50px}.pane{min-width:0;background:var(--s);border-radius:10px;overflow:hidden}.pane h2{position:sticky;top:60px;margin:0;padding:14px 18px;background:var(--s2);font-size:15px;font-weight:500}.pane pre{margin:0;padding:18px;overflow:auto;white-space:pre-wrap;word-break:break-word;font:12px/1.65 ui-monospace,SFMono-Regular,Menlo,monospace;color:#cdd1d4}.meta{font-size:12px;color:var(--m);margin-top:5px}.hit{background:#594a1d;color:#fff3c7}@media(max-width:800px){.cols,.stats{grid-template-columns:1fr}.pane h2{top:60px}}</style></head><body><header><div class="wrap"><div class="pi">EXACT SOURCE SNAPSHOTS</div><h1>Pi and OMP,<br>the full prompts.</h1><p class="lead">No summaries below. The left side is Pi’s complete rendered default base prompt for its four default tools. The right side is OMP’s complete default Handlebars system-prompt template.</p><div class="note"><b>Why one side has placeholders:</b> Pi builds a compact prompt directly. OMP renders a large conditional template based on tools, personality, rules, memory, subagents, security, and settings. A single universal rendered OMP prompt does not exist. The template below is the complete source that produces it. Use Pix <code>/prompt</code> to export the exact active Pi/Pix prompt, including your AGENTS.md and skills.</div><div class="stats"><div class="stat"><span class="pi">PI DEFAULT</span><b>${pi.length.toLocaleString()} chars</b><span>${pi.split("\n").length} lines · ${words(pi).toLocaleString()} words</span></div><div class="stat"><span class="omp">OMP TEMPLATE</span><b>${omp.length.toLocaleString()} chars</b><span>${omp.split("\n").length} lines · ${words(omp).toLocaleString()} words</span></div></div></div></header><div class="toolbar"><div class="wrap"><input id="q" placeholder="Search both prompts…" aria-label="Search prompts"></div></div><main class="wrap cols"><section class="pane"><h2><span class="pi">Pi</span> · complete rendered base <div class="meta">Pi 0.84.4 · default tools · no personal/project context</div></h2><pre id="pi">${esc(pi)}</pre></section><section class="pane"><h2><span class="omp">OMP</span> · complete default template <div class="meta">source commit 39cf639 · conditional sections preserved</div></h2><pre id="omp">${esc(omp)}</pre></section></main><script>const raw={pi:${JSON.stringify(pi)},omp:${JSON.stringify(omp)}};const esc=s=>s.replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));document.querySelector('#q').addEventListener('input',e=>{const q=e.target.value.trim();for(const id of ['pi','omp']){let s=esc(raw[id]);if(q){const safe=q.replace(/[.*+?^\${}()|[\]\\]/g,'\\$&');s=s.replace(new RegExp(safe,'gi'),m=>'<mark class="hit">'+m+'</mark>')}document.querySelector('#'+id).innerHTML=s}})</script></body></html>`;
await writeFile(join(root,"docs/system-prompts.html"),html);
console.log(`wrote docs/system-prompts.html`);
