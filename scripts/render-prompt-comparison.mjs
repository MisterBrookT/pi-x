import { readFile, writeFile } from "node:fs/promises";
import { dirname, join } from "node:path";
import { fileURLToPath } from "node:url";

const root = join(dirname(fileURLToPath(import.meta.url)), "..");
const pi = await readFile(join(root, "docs/prompts/pi-default.txt"), "utf8");
const pix = await readFile(join(root, "docs/prompts/pix-default.txt"), "utf8");
const esc = (value) => value.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;");
const words = (value) => value.trim().split(/\s+/).filter(Boolean).length;

const html = `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>System prompts — naive Pi vs Pix</title><style>:root{color-scheme:dark;--bg:#0b0c0d;--s:#151719;--s2:#1e2124;--t:#f1f2f3;--m:#9da3a9;--pi:#9da3a9;--pix:#9fe5b4}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--t);font:14px/1.55 Inter,-apple-system,sans-serif}.wrap{width:min(1500px,calc(100% - 28px));margin:auto}header{padding:52px 0 28px}h1{font-size:clamp(36px,6vw,72px);line-height:1;letter-spacing:-.055em;font-weight:500;margin:12px 0 18px}.lead,.meta{color:var(--m)}.lead{font-size:18px;max-width:900px}.note{background:var(--s);padding:18px 20px;border-radius:10px;margin-top:22px}.stats,.cols{display:grid;grid-template-columns:1fr 1fr;gap:10px}.stats{margin:24px 0}.stat{background:var(--s);border-radius:10px;padding:16px}.stat b{font-size:28px;font-weight:500;display:block}.pi{color:var(--pi)}.pix{color:var(--pix)}.toolbar{position:sticky;top:0;background:rgba(11,12,13,.95);backdrop-filter:blur(14px);padding:10px 0;z-index:2}.toolbar input{width:100%;height:40px;border:0;border-radius:8px;background:var(--s2);color:var(--t);padding:0 13px;font:14px monospace}.cols{padding:12px 0 50px}.pane{min-width:0;background:var(--s);border-radius:10px;overflow:hidden}.pane h2{position:sticky;top:60px;margin:0;padding:14px 18px;background:var(--s2);font-size:15px;font-weight:500}.pane pre{margin:0;padding:18px;overflow:auto;white-space:pre-wrap;word-break:break-word;font:12px/1.65 ui-monospace,SFMono-Regular,Menlo,monospace;color:#cdd1d4}.meta{font-size:12px;margin-top:5px}.hit{background:#594a1d;color:#fff3c7}@media(max-width:800px){.cols,.stats{grid-template-columns:1fr}}</style></head><body><header><div class="wrap"><div class="pix">PUBLIC-SAFE BASELINES</div><h1>Naive Pi and Pix,<br>the full prompts.</h1><p class="lead">The same clean environment, before and after Pix adds its tools and concise operating guidance.</p><div class="note">Personal instructions, project instructions, credentials, and session context are excluded. Use <code>/prompt</code> for the exact effective prompt in your current session.</div><div class="stats"><div class="stat"><span class="pi">NAIVE PI</span><b>${pi.length.toLocaleString()} chars</b><span>${pi.split("\n").length} lines · ${words(pi).toLocaleString()} words</span></div><div class="stat"><span class="pix">PIX</span><b>${pix.length.toLocaleString()} chars</b><span>${pix.split("\n").length} lines · ${words(pix).toLocaleString()} words</span></div></div></div></header><div class="toolbar"><div class="wrap"><input id="q" placeholder="Search both prompts…" aria-label="Search prompts"></div></div><main class="wrap cols"><section class="pane"><h2><span class="pi">Naive Pi</span><div class="meta">default coding tools</div></h2><pre id="pi">${esc(pi)}</pre></section><section class="pane"><h2><span class="pix">Pix</span><div class="meta">focused tools · concise guidance</div></h2><pre id="pix">${esc(pix)}</pre></section></main><script>const raw={pi:${JSON.stringify(pi)},pix:${JSON.stringify(pix)}};const esc=s=>s.replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));document.querySelector('#q').addEventListener('input',e=>{const q=e.target.value.trim();for(const id of ['pi','pix']){let s=esc(raw[id]);if(q){const safe=q.replace(/[.*+?^\${}()|[\]\\]/g,'\\$&');s=s.replace(new RegExp(safe,'gi'),m=>'<mark class="hit">'+m+'</mark>')}document.querySelector('#'+id).innerHTML=s}})</script></body></html>`;

await writeFile(join(root, "docs/system-prompts.html"), html);
console.log("wrote docs/system-prompts.html");
